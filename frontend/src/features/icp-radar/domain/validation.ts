// Signal validation owns effective score transitions and local review ranking.
export {
  loadSignalValidationOverlay,
  signalValidationKey,
  validationForCandidate,
  validationRank,
  validationStatusKey,
  validationTone,
} from '../validationModel';

